include "base-test.conf"

kafka {
  input.topic = "sl-improvement-observation-submission-dev"
  groupId = "sl-improvement-observation-consumer-group-dev"
  output.topic = "sl-metabase-observation-dashboard-dev"
}

task {
  sl.observation.stream.parallelism = 1
  sl.metabase.observation.dashboard.parallelism = 1
}

postgres{
  host = "localhost"
  port = "5432"
  username = "postgres"
  password = "postgres"
  database = "test"
  tables = {
    domainsTable = ${job.env}"_domain"
    questionsable = ${job.env}"_questions"
    tasksTable = ${job.env}"_tasks"
    dashboardMetadataTable = ${job.env}"_dashboard_metadata"
  }
}

reports{
  enabled = ["admin", "state", "district", "program"]
}